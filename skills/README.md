# Production skills

`foreman-agent` is the production router.
`foreman-supervisor` describes the bounded supervision/restart pass.
`foreman-recovery` describes confirmed dead/missing worker handoff.

The files under `legacy/` are retained for historical parity work and are not production entry points.
