{
  "schemaVersion": 1,
  "router": {
    "tool": "codex",
    "command": [
      "codex",
      "exec",
      "--sandbox",
      "read-only",
      "--ephemeral"
    ],
    "model": "gpt-6-luna",
    "effort": null,
    "whenToUse": "Classify every new Foreman task and select one configured worker profile."
  },
  "default": "codex-luna",
  "groups": {
    "ordinary": {
      "whenToUse": "Mechanical edits, initial bug investigation and fixes, features within an existing design, UI, tests, and reviews. Use when the brief does not establish a need for deep redesign; file count alone does not make a task deep.",
      "profiles": ["codex-luna", "omp-luna", "claude-sonnet", "codex-sol"]
    },
    "deep": {
      "whenToUse": "New architecture or data-model design, difficult concurrency or security reasoning, or a bug still unexplained after earlier investigation. File count or initial investigation alone is not enough.",
      "profiles": ["claude-opus"]
    }
  },
  "profiles": {
    "codex-sol": {
      "tool": "codex",
      "command": [
        "codex",
        "--yolo"
      ],
      "model": "gpt-6-sol",
      "effort": "medium",
      "whenToUse": "Use for tooling-heavy builds, tests, dependencies, CI, or scripted batch edits when this profile is explicitly requested.",
      "isActive": false
    },
    "codex-luna": {
      "tool": "codex",
      "command": [
        "codex",
        "--yolo"
      ],
      "model": "gpt-6-luna",
      "effort": "max",
      "whenToUse": "Preferred within its group unless another tool is explicitly requested or needs native capabilities this tool lacks."
    },
    "omp-luna": {
      "tool": "omp",
      "command": [
        "omp",
        "--auto-approve"
      ],
      "model": "openai-codex/gpt-6-luna",
      "effort": "max",
      "whenToUse": "Use when OMP is explicitly requested or its native capabilities are needed."
    },
    "claude-sonnet": {
      "tool": "claude",
      "command": [
        "claude",
        "--dangerously-skip-permissions"
      ],
      "model": "claude-sonnet-5",
      "effort": "high",
      "whenToUse": "Use when Claude is explicitly requested or codex-luna is not active."
    },
    "claude-opus": {
      "tool": "claude",
      "command": [
        "claude",
        "--dangerously-skip-permissions"
      ],
      "model": "claude-opus-5-5",
      "effort": "high",
      "whenToUse": "Use for architecture, migration, concurrency, or security work in this group."
    }
  }
}